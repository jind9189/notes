


Assembly: compiled code is either ``exe/dll`` and called assembly. IL code is execute with CLR.
### Variables

- Variable is a named memory location in RAM, to store a particular type of value, during the program execution
- Variable will stored in stack
- For every method call, a new stack will be created
- The variable value can be changed any no of times.
- The variables must be declared before its usage
- The variables must be initiated before reading its value.
- Variable’s data type should be specified while declaring the variable. It can’t change later.
- The stack (along with its variable) will be deleted automatically, at the end of method execution.
    
Syntax : 

```
DataType VariableName:
DataType Variable = Defaultname;
Variable = newValue
```

### Data Type

### Condition Control
Control statements are used to control the program execution flow